apiVersion: apps/v1
kind: Deployment
metadata:
  name: refund-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: refund-service
  template:
    metadata:
      labels:
        app: refund-service
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: refund-service
          image: your-registry/refund-service:latest
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 10001
            runAsUser: 10001
          ports:
            - containerPort: 8086
            - containerPort: 9086
          envFrom:
            - configMapRef:
                name: go-micro-config
            - secretRef:
                name: go-micro-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /healthz
              port: 8086
            failureThreshold: 30
            periodSeconds: 2
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8086
            initialDelaySeconds: 5
            failureThreshold: 3
            periodSeconds: 10
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8086
            initialDelaySeconds: 5
            failureThreshold: 3
            periodSeconds: 10
            timeoutSeconds: 2
---
apiVersion: v1
kind: Service
metadata:
  name: refund-service
spec:
  selector:
    app: refund-service
  ports:
    - name: http
      port: 8086
      targetPort: 8086
    - name: grpc
      port: 9086
      targetPort: 9086
  type: ClusterIP
